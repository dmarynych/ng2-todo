# language setting
language: node_js

# version numbers, testing against two versions of node
node_js:
    - 6.6.0

branches:
  only:
    - tutorial

script: ls

after_success :
    #- rsync -az ./app stark@up2.onjs.net:~/hosa-front
    #- rsync -az ./webpack stark@up2.onjs.net:~/hosa-front
    #- rsync -az ./package.json stark@up2.onjs.net:~/hosa-front/package.json
    #- ssh -t stark@up2.onjs.net 'cd ./hosa-front; /home/stark/.nvm/versions/v4.0.0/bin/npm install'
    #- ssh -t stark@up2.onjs.net 'cd ./hosa-front; /home/stark/.nvm/versions/v4.0.0/bin/npm run build'
